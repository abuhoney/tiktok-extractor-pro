# ============================================================
# TikTok Extractor Pro v5.0.36 — Python dependencies
# ============================================================
# Core web framework
flask==3.1.3
waitress==3.0.2

# HTTP / parsing
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
urllib3==2.5.0
pysocks==1.7.1

# Video / media extraction
yt-dlp==2026.8.19

# Playwright (REAL X-Bogus + REAL csrf/wid/nonce extraction via real webmssdk.js)
# After install: playwright install chromium --with-deps
playwright==1.50.0

# Optional: DNS fallback (used by onlinetiktok.py)
dnspython==2.7.0

# Z-AI SDK for screenshot analysis (used by attached_file_processor.py)
z-ai-web-dev-sdk>=0.1.0
