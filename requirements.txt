# ============================================================
# TikTok Extractor Pro v5.0.38 — Python dependencies
# ============================================================
# Core web framework
flask==3.1.3
waitress==3.0.2

# HTTP / parsing
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
urllib3==2.5.0
pysocks==1.7.1

# Video / media extraction
yt-dlp==2026.8.19

# Playwright (REAL X-Bogus generation via real webmssdk.js)
# After install: playwright install chromium
# Note: If Chromium install fails (e.g. on Render free tier),
# the code gracefully falls back to xbogus.py (Python port).
playwright==1.50.0

# Optional: DNS fallback (used by onlinetiktok.py)
dnspython==2.7.0
